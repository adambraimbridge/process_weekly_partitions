version: 2
jobs:
  build:
    working_directory: ~/workspace
    machine: true
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      -run:
          name: Install tools
          command: 
      - run:
          name: Deploy to S3 if tests pass and branch is Master
          command: aws s3 sync ~/workspace s3://dev.ft.dw.load.mohammed.test --delete
